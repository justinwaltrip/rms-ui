import "./InfoBar.css";
import { FC } from "react";

const InfoBar: FC = () => {
  return (
    <div className="info-bar">
      <div className="title">title</div>
    </div>
  );
};

export default InfoBar;
